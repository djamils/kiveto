{# Path B 422: replace just the form body with the re-rendered partial. #}
<turbo-stream action="replace" target="new-client-form-body">
  <template>
    {{ include('clinic/clients/list/_form_client_body.html.twig', {form: form}) }}
  </template>
</turbo-stream>
