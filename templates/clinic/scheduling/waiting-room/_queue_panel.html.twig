{# Left queue panel: shell only — all content rendered by the Turbo Frame #}

<div class="queue-panel" id="queue-panel">
  <turbo-frame id="waiting-queue"
               src="{{ path('clinic_scheduling_waitingroom_queue') }}"
               refresh="morph"
               data-turbo-action="replace"
               data-controller="auto-refresh"
               data-auto-refresh-interval-value="20000">
    <div class="wr-frame-loading">Chargement&hellip;</div>
  </turbo-frame>
</div>
