{#
 # Cockpit overlays.
 #
 # Modals themselves are built by assets/js/pages/consultation.js (they are
 # data-driven, so a static shell would only duplicate their markup). What stays
 # here are the three inline annotation slots each SOAP quad writes into.
 #}
<div class="annotation" id="qO-annotation"></div>
<div class="annotation" id="qA-annotation"></div>
<div class="annotation" id="qP-annotation"></div>
