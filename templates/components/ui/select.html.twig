{#
 # Kiveto — templates/components/ui/select.html.twig
 # ──────────────────────────────────────────────────
 # Custom animated select dropdown (Stimulus controller).
 # Uses the Kiveto design system CSS classes for consistency.
 #
 # Parameters:
 #   name         string   Form field name (required)
 #   options      array    [ { value: string, label: string } ]  (required)
 #   value        string   Pre-selected value (default: '')
 #   placeholder  string   Placeholder text (default: 'Sélectionner...')
 #   searchable   bool     Show search input (default: false)
 #   clearable    bool     Show clear button when value is set (default: false)
 #   size         string   sm | md  (default: md — matches .form-input height)
 #   id           string   Custom id (default: auto from name)
 #   disabled     bool     (default: false)
 #   extra_class  string   Additional classes on the wrapper
 #
 # Usage:
 #   {{ include('components/ui/select.html.twig', {
 #     name: 'status',
 #     placeholder: 'Choisir un statut',
 #     options: [
 #       { value: 'active', label: 'Actif' },
 #       { value: 'inactive', label: 'Inactif' },
 #     ],
 #     value: 'active',
 #     searchable: true,
 #     clearable: true,
 #   }) }}
 #}

{% set _id = id|default('select-' ~ name) %}
{% set _value = value|default('') %}
{% set _placeholder = placeholder|default('Sélectionner...') %}
{% set _searchable = searchable|default(false) %}
{% set _clearable = clearable|default(false) %}
{% set _disabled = disabled|default(false) %}
{% set _size = size|default('md') %}
{% set _selected_label = '' %}

{# Find pre-selected label #}
{% for opt in options %}
  {% if opt.value is same as(_value) %}
    {% set _selected_label = opt.label %}
  {% endif %}
{% endfor %}

<div
  class="ki-select{{ extra_class is defined ? ' ' ~ extra_class : '' }}"
  data-controller="select"
  data-select-placeholder-value="{{ _placeholder }}"
  {% if direction is defined and direction == 'up' %}data-select-direction-value="up"{% endif %}
  data-action="keydown->select#onKeydown"
>
  {# Hidden input for form submission #}
  <input
    type="hidden"
    name="{{ name }}"
    value="{{ _value }}"
    id="{{ _id }}"
    data-select-target="hidden"
  />

  {# Trigger button — styled like .form-input #}
  <button
    type="button"
    class="ki-select-trigger{{ _size == 'sm' ? ' ki-select-trigger--sm' : '' }}{{ _disabled ? ' is-disabled' : '' }}"
    data-select-target="trigger"
    data-action="click->select#toggle"
    role="combobox"
    aria-expanded="false"
    aria-haspopup="listbox"
    aria-controls="{{ _id }}-listbox"
    {{ _disabled ? 'disabled' : '' }}
  >
    <span
      data-select-target="label"
      class="ki-select-label{{ _selected_label ? '' : ' is-placeholder' }}"
    >{{ _selected_label ? _selected_label : _placeholder }}</span>

    <span class="ki-select-icons">
      {# Clear button #}
      {% if _clearable %}
        <button
          type="button"
          class="ki-select-clear{{ _value ? '' : ' hidden' }}"
          data-select-target="clear"
          data-action="click->select#clear"
          aria-label="Effacer"
        >
          <svg width="12" height="12" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      {% endif %}

      {# Chevron #}
      <svg class="ki-select-chevron" width="10" height="6" fill="none" viewBox="0 0 10 6">
        <path d="M1 1l4 4 4-4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </span>
  </button>

  {# Dropdown panel #}
  <div
    class="ki-select-dropdown hidden"
    data-select-target="dropdown"
    role="listbox"
    id="{{ _id }}-listbox"
    aria-label="{{ _placeholder }}"
  >
    {# Search input #}
    {% if _searchable %}
      <div class="ki-select-search">
        <input
          type="text"
          class="form-input form-input-sm"
          placeholder="Rechercher..."
          data-select-target="search"
          data-action="input->select#filter"
          autocomplete="off"
        />
      </div>
    {% endif %}

    {# Options list #}
    <div class="ki-select-options">
      {% for opt in options %}
        <button
          type="button"
          class="ki-select-option"
          data-select-target="option"
          data-action="click->select#select"
          data-value="{{ opt.value }}"
          data-label="{{ opt.label }}"
          role="option"
          aria-selected="{{ opt.value is same as(_value) ? 'true' : 'false' }}"
        >
          {# Check icon for selected state #}
          <svg
            class="ki-select-check{{ opt.value is same as(_value) ? '' : ' hidden' }}"
            width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
          </svg>
          <span class="truncate">{{ opt.label }}</span>
        </button>
      {% endfor %}
    </div>
  </div>
</div>
